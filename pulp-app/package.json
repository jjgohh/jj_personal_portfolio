{
  "name": "pulp-landing-react",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "PULP — full-spectrum tocotrienol vitamin E landing page (Vite + React + framer-motion)",
  "scripts": {
    "dev": "vite",
    "build": "vite build && vite build --config vite.ssr.config.js && node tools/prerender-dist.mjs",
    "preview": "vite preview",
    "build:artifact": "vite build --config vite.artifact.config.js && vite build --config vite.ssr.config.js && node tools/make-artifact.mjs",
    "check": "node tools/check-site.mjs"
  },
  "dependencies": {
    "@gsap/react": "^2.1.2",
    "gsap": "^3.15.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "three": "^0.160.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.1",
    "vite": "^5.4.0",
    "vite-plugin-singlefile": "^2.3.3"
  }
}
