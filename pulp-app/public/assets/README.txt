PULP — asset slots
Drop these files into this /assets folder and the placeholders are replaced automatically:

  origin.jpg           4:5   Palm-fruit origin photo (Plate A)
  origin.mp4 + .webm   4:5   Muted, looping origin film (Plate E)

Until they exist, the page shows labelled placeholder "plates" (this is expected).
